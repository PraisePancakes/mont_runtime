#pragma once
// -(a + (b - (c * 3))) recursively find the deepest group, operate that expression first than plug into the 2nd deepest group etc.. all the way to the first group
class ExpressionVisitor
{
    
};